import {expect,it} from 'vitest';import {JSDOM} from 'jsdom';import {compile,getAuthoringReference} from '../index';
it.each(['avatar','skeleton','footnotes','breadcrumb','pagination','sidebar'])('provides a useful starter for %s',async name=>{const ref=await getAuthoringReference();const example=ref.components.find(c=>c.name===name)?.example;expect(example).toBeTruthy();const result=await compile(example!);expect(result.metadata.warnings).toEqual([]);const dom=new JSDOM(result.html);try{const d=dom.window.document;if(name==='avatar')expect(d.querySelector('[data-component="avatar"]')?.textContent).toBe('AM');else if(name==='skeleton')expect(d.querySelector('[data-component="skeleton"]')?.classList.contains('h-32')).toBe(true);else if(name==='footnotes'){expect(d.querySelector('a[data-footnote-ref]')).not.toBeNull();expect(d.querySelector('section[data-footnotes]')).not.toBeNull();}else expect(d.querySelector('[data-component="'+name+'"] a')).not.toBeNull();for(const link of d.querySelectorAll('a[href^="#"]'))expect(d.getElementById(decodeURIComponent(link.getAttribute('href')!.slice(1)))).not.toBeNull();}finally{dom.window.close();}});
